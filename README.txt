For å kjøre applikasjonen, utfør følgende kommando (baset på gradle i linux):
./gradlew build && java -jar build/libs/gs-web-content-0.1.0.jar

Applikasjonen vises i webbrowser via følgende stier:

Prismotor(liste):
http://localhost:8080/

Priskalkulator:
http://localhost:8080/kalkulator
